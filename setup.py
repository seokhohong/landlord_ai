from setuptools import setup

setup(name='landlord_ai',
      version='0.1',
      description='Landlord AI Core',
      license='GNU',
      packages=['landlord'],
      install_requires=[],
      zip_safe=False)